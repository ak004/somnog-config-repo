# Shared by every location. X-Forwarded-* is not decoration: the gateway trusts
# exactly one hop (app.set('trust proxy', 1)) to rate-limit per client ip
# instead of per proxy, and Next.js builds absolute urls from them.
proxy_http_version 1.1;
proxy_set_header Host              $host;
proxy_set_header X-Real-IP         $remote_addr;
proxy_set_header X-Forwarded-For   $proxy_add_x_forwarded_for;
proxy_set_header X-Forwarded-Proto $scheme;
proxy_set_header X-Forwarded-Host  $host;
proxy_set_header Upgrade           $http_upgrade;
proxy_set_header Connection        $connection_upgrade;
proxy_read_timeout 60s;
